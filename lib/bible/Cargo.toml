[package]
name = "bible"
version = "0.1.0"
edition = "2021"

# Dependencies specific to the bible library can be added here
[dependencies]
csv = "1.3.0"
serde = { version = "^1.0.210", features = ["derive"] }
regex = "1.10.6"
rand = "0.8.5"
