[package]
name = "bible"
version = "0.1.0"
edition = "2021"

# Dependencies specific to the bible library can be added here
[dependencies]
csv = "1.3.0"
serde = { version = "^1.0.196", features = ["derive"] }